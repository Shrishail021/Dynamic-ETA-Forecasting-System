# TODO(Antigravity): wire these into app/core/config.py via pydantic-settings
# once real persistence/auth replaces the current stubs.
DATABASE_URL=sqlite:///./eta_system.db
SECRET_KEY=change-me
ADMIN_DEMO_TOKEN=demo-admin-token-change-me
